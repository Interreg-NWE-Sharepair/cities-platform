{% set baseUrl = craft.app.config.general.aliases.baseUrl %}
{% extends "_mail/_layout" %}

{# subject: Bedankt om je te registreren als repair hero! #}
{# fromEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# fromName: {{ siteInfo.cityName }} #}
{# replyToEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# cc:  #}
{# bcc: #}
{# includeAttachments: true #}
{# presetAssets: 42,67 #}
{# description: #}

	{% block title %}
{{ 'Bedankt om je te registreren als repair hero!'|t }}
{% endblock %}

	{% block content %}
		<p>{{ 'Hey'|t }}
		{{ firstName }}
	,
	</p>
		<p>
	{{ 'We hebben jouw repair hero aanmelding goed ontvangen. Bedankt om je aan te melden! Een van de platform administrators zal je aanmelding bekijken en je contacteren om je te introduceren in de community en jouw profiel te activeren.'|t }}
	</p>
		<p>
	{{ 'Een kopie van jouw aanmelding:'|t }}
	</p>
		<ul>
			{% for field in allFields|filter(field => field.handle != 'terms' and field.handle != 'data') %}
				<li>
					<strong>
				{{ field.label }}
				</strong>
				<br>
			{{ field.getValueAsString() }}
		</li>
	{% endfor %}
	</ul>
</p>{% endblock %}
