{% set baseUrl = craft.app.config.general.aliases.baseUrl %}
{% extends "_mail/_layout" %}

{# subject: Thank you for nominating someone as a repair hero! #}
{# fromEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# fromName: {{ siteInfo.cityName }} #}
{# replyToEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# cc:  #}
{# bcc: #}
{# includeAttachments: true #}
{# presetAssets: 42,67 #}
{# description: #}

	{% block title %}
{{ 'Thank you for nominating someone as a repair hero!'|t }}
{% endblock %}

	{% block content %}
		<p>{{ 'Hi'|t }}
		{{ yourFirstName }},
	</p>
		<p>
	{{ 'We received your repair hero nomination. Thank you for nominating someone! One of the platform administrators will review your nomination and do the necessary to arrange everything.'|t }}
	</p>
		<p>
	{{ 'A copy of your application:'|t }}
	</p>
		<ul>
			{% for field in allFields|filter(field => field.handle != 'terms' and field.handle != 'data') %}
				<li>
					<strong>
				{{ field.label }}
				</strong>
				<br>
			{{ field.getValueAsString() }}
		</li>
	{% endfor %}
	</ul>
</p>{% endblock %}
