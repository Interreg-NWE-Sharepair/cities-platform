{% set baseUrl = craft.app.config.general.aliases.baseUrl %}
{% extends "_mail/_layout" %}

{# subject: Thank you for registering as a repair hero! #}
{# fromEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# fromName: {{ siteInfo.cityName }} #}
{# replyToEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# cc:  #}
{# bcc: #}
{# includeAttachments: true #}
{# presetAssets: 42,67 #}
{# description: #}

	{% block title %}
{{ 'Thank you for registering as a repair hero!'|t }}
{% endblock %}

	{% block content %}
		<p>{{ 'Hi'|t }}
		{{ firstName }}
	,
	</p>
		<p>
	{{ 'We received your repair hero application. Thank you for applying! One of the platform administrators will review your application, and get in touch to introduce you to the community and activate your repair hero profile.'|t }}
	</p>
		<p>
	{{ 'A copy of your application:'|t }}
	</p>
		<ul>
			{% for field in allFields|filter(field => field.handle != 'terms' and field.handle != 'data') %}
				<li>
					<strong>
						{{ field.label }}
					</strong>
				<br>
			{{ field.getValueAsString() }}
		</li>
	{% endfor %}
	</ul>
</p>{% endblock %}
