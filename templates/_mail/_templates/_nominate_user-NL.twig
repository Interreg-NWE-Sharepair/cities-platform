{% set baseUrl = craft.app.config.general.aliases.baseUrl %}
{% extends "_mail/_layout" %}

{# subject: Bedankt om iemand te nomineren als repair hero! #}
{# fromEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# fromName: {{ siteInfo.cityName }} #}
{# replyToEmail: {{ craft.app.projectConfig.get('email.fromEmail') }} #}
{# cc:  #}
{# bcc: #}
{# includeAttachments: true #}
{# presetAssets: 42,67 #}
{# description: #}

	{% block title %}
{{ 'Bedankt om iemand te nomineren als repair hero!'|t }}
{% endblock %}

	{% block content %}
		<p>{{ 'Hey'|t }}
		{{ yourFirstName }},
	</p>
		<p>
	{{ 'We hebben jouw repair hero nominatie goed ontvangen. Bedankt om iemand te nomineren! Een van de platform administrators zal je nominatie bekijken en het nodige doen om dit in orde te brengen.'|t }}
	</p>
		<p>
	{{ 'Een kopie van jouw aanmelding:'|t }}
	</p>
		<ul>
			{% for field in allFields|filter(field => field.handle != 'terms' and field.handle != 'data') %}
				<li>
					<strong>
				{{ field.label }}
				</strong>
				<br>
			{{ field.getValueAsString() }}
		</li>
	{% endfor %}
	</ul>
</p>{% endblock %}
